var first_name = "Ricky";
var last_name = "Bobby";
var age = 45;
var first = true;
var last = false;
var bob;


if ( age <= 40 ){
  //if the value of age is less than or equal to 40
} else if ( (age > 40) &&  (age < 50) ){
  // if age is greater than 40 and less than 50
} else if( age < 0 ){
  // prevent something from happening
} else {
  //catchall
}
