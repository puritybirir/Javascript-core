var characters = ['Luke', 'Leia', 'Han', 'Chewie'];

console.log(characters);
console.log(characters.length)
console.log(characters[0]);

var last_item = characters[characters.length - 1];
console.log(last_item);

// Adding to an array

characters.push('Anakin');
characters.push('Obiwan');
console.log(characters);

console.log(last_item);

//Removing from an array
characters.pop();
characters.pop()
characters

//Adding from the front
characters.unshift('C3PO')
characters.unshift('R2D2')
characters

// Removing first item
characters.shift();
characters;

console.log(characters.shift())



